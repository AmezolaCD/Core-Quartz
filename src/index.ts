/** Punto de entrada del servidor: lee la configuración y escucha. */
import { crearApp } from './app.ts';

export async function arrancar(): Promise<void> {
  throw new Error('no implementado: arrancar');
}

export { crearApp };
